public enum TreeType {
//    enum is a special class that represents a group of constants. BY USING ENUM, NO ONE CAN CREATE A DIFFERENT TYPE OF THREE OTHER THAN THE ONES BELOW
    OAK,
    MAPLE,
    PECAN,
    WALNUT,
    PINE
}
